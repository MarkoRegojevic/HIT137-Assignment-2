import os

# makes the tokens from expression
def tokenize(text):
    i=0
    while i < len(text):
        char=text[i]
if char.isspace():
    i==1+ 
    continue

if char.isdigit():
    start=i
    while i < len(text) and text[i].isdigit():
        